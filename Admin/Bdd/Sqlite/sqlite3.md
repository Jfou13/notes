# Sqlite3

## Si sqlite3 n'est pas installé et qu'on veut voir une base

### Sur la cible (copiez le texte qui s'affiche) :
```shell
base64 reactor.db
```
### Sur votre machine Kali :
```shell
echo "COLLEZ_ICI_LE_TEXTE" | base64 -d > reactor.db
```
